# cerase-tasks MCP — Python dependency lock (PROJ-3).
# A thin proxy to the control-plane PROJ-2 internal endpoints.
# ⚠️ `mcp` resta alla 1.x finche' il codice non e' migrato: nella 2.x
# `FastMCP` si chiama `MCPServer`, e `server.py` qui usa la 1.x. Il bump a
# `>=2.1.1,<3.0` e' passato il 2026-09-08 (PR #58, gruppo `python`) e ha reso
# questo server non avviabile: l'immagine si costruisce, l'import muore. Alzare
# il pin senza migrare il codice non e' un aggiornamento, e' un guasto differito.
mcp>=1.0,<2.0
mcp-proxy>=0.3,<1.0
httpx>=0.28.1,<1.0
